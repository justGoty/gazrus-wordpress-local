# Товарные записи

Один проверенный товар хранится в одном JSON-файле. Формат и порядок публикации описаны в `docs/rebuild/CATALOG_CONTRACT.md`.

До сверки с паспортом или официальным источником запись не переводится в `published` и не получает придуманные характеристики.
